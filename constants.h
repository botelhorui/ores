#pragma once

//Screen width pixeis
const int SCREEN_WIDTH = 640;

//Screen height pixeis
const int SCREEN_HEIGHT = 480;

// Matrix width in number of blocks
const int MATRIX_WIDTH = 17;

// Matrix height in number of blocks
const int MATRIX_HEIGHT = 12;

// Number of blocks of space from the left of the screen and the matrix
const int MATRIX_LEFT_BORDER = 2;

// Number of blocks of space from botttom of the screen and the matrix
const int MATRIX_BOT_BORDER = 1;

// Number of the matrix column where the first blocks columns start
const int GAME_FIRST_COLUMN = 8;

// Width in pixeis of the highlight border around each block
const int HIGHLIGHT_BORDER_WIDTH = 3;

// Number of pixeis per block side
const int TEXTURE_SIDE = 32;

// Ammount of seconds between right column insertion
const int TIMER_CLICKS = 5 * 1000;

// Geometric properties of the timer
const int TIMER_POS_X = 40;
const int TIMER_POS_Y = 10;
const int TIMER_HEIGHT = 25;
const int TIMER_WIDTH = 100;





